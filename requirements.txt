# Minimal deps for Vercel (proxy mode). Vercel installs this file automatically.
# Set env BACKEND_URL to your full API (Fly.io/Render). For local/full API use: pip install -r requirements-full.txt
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
httpx>=0.24.0
